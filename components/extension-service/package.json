{
  "name": "@cn-gitpod/extension-service",
  "version": "0.0.1",
  "description": "Typescript Express.js server for Extension Service",
  "scripts": {
    "p:deploy": "npx prisma migrate deploy",
    "p:generate": "npx prisma generate",
    "p:migrate": "npx prisma migrate dev",
    "p:studio": "npx prisma studio",
    "clean": "rimraf lib",
    "start": "node ./lib/app.js",
    "build": "yarn lint && npx tsc",
    "build:clean": "yarn clean && yarn build",
    "rebuild": "yarn build:clean",
    "lint": "yarn tslint -p .",
    "updatearm": "yarn rebuild && leeway build components/extension-service:docker -D version=arm-edge-case --dont-test",
    "testdigest": "npx ts-node src/utils/digest.ts"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@cn-gitpod/extension-service-api": "0.0.1",
    "@grpc/grpc-js": "^1.8.1",
    "@grpc/proto-loader": "^0.7.4",
    "@prisma/client": "^4.8.1",
    "dotenv": "^16.0.3",
    "prisma": "^4.8.1",
    "zod": "^3.20.2"
  }
}
